# Hideout + MissionBoard (0M-D1B-RT)

**Not executed** — no running Godot instance / no CLI binary.

**Inferred from code (smoke substitute):**

- `project.godot` `run/main_scene` → `res://scenes/MainMenu.tscn`
- `SceneManager.start_mission` uses `GameState.get_mission_scene_path` and debug override for `taco_bell_drop` → `TacoBellIso_Editable.tscn`

Re-test in editor when available.
