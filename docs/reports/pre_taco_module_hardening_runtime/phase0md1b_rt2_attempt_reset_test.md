# 0M-D1B-RT2 — Reset / beam / counters (Phase 8)

## Contract (static)

Prior D1B work documents `reset_mission_runtime_for_new_attempt()` on `IsoMissionBase` — **not invoked** in this RT2 GRB session.

## Runtime

Beam one-shot, code gate vs beam separation, death/retry flows, and per-attempt counter resets were **not reached** (no gameplay automation beyond scene loads).

## Persistent progress

No save/load or mission-complete calls were made during automation.
