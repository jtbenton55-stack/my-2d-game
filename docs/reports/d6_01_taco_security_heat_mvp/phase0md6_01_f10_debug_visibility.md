# F10 debug visibility

- **`IsoMissionDebugPanel`** appends `--- Security (D6-01) ---` block: per-kind adapter counters, last up to 8 events, deferred notes.
- **Player HUD:** unchanged (no security clutter on mission HUD strip).
- **Scroll:** existing `ScrollContainer` on compact panel retained.
