# 0M-D2A — Test harness

Run `res://scenes/hideout/tools/PlayerSprintInputTest_0MD2A.tscn` (F6). Output lists:

- `sprint` action events (expect two `InputEventKey`: 32 and 4194326).
- `PlayerStaminaController` fields and a short stamina tick with live `is_sprint_requested()`.
