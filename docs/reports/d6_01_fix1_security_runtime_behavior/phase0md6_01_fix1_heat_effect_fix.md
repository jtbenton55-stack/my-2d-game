# Heat effect fix

Minimal connection: **stronger camera multipliers at heat 1** so second attempt is measurably harsher without new persistence fields. All other heat steps still use existing `_apply_heat_profile` table.
