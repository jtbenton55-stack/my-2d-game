# Phase 3 — Pause scheme cleanup (implementation)

- `MissionSchemeCardFormatter.gd` added.
- `pause_menu.gd` Scheme tab uses formatter only.
- `MissionSchemeBridge` renames `effects_note` → `scheme_debug_note` (technical, for F10).
