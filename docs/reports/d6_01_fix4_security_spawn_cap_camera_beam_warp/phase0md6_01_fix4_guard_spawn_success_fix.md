# Guard spawn success / visibility

- Reinforcement path now always configures the **returned** guard instance.
- Patrol fallback remains via **`_assign_fallback_patrol_for_security_guard`** when supported.

See `phase0md6_01_fix4_guard_spawn_success_fix.json`.
