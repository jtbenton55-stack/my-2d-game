# iso_vertical_slice — **TEMPORARY PROTOTYPE ART**

This folder holds **throwaway placeholder graphics** for the internal **iso_vertical_slice** mission. It exists only to prove **standalone `TileMapLayer` + isometric `TileSet` + collision** wiring.

- **`prototype_iso_atlas.png`** — procedurally drawn placeholder (two 64×64 blocks: “grass” / “wall”). Replace with a licensed pack when approved.
- **`IsoVerticalSlice.tres`** — isometric `TileSet` referencing that atlas. Floor tile: atlas coords `(0,0)`. Wall tile (with physics polygon): `(1,0)`.

**Do not ship** this art as final content.
