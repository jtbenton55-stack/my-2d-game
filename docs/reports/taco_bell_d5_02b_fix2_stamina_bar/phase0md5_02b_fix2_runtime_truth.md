# Phase 2 — Runtime truth

`grb_ping` returned **“Bridge not connected. Launch the game first.”** No live inspection of `SprintStaminaBar.visible`, sizes, or drain animation was possible in this session.

Manual playtest (user machine) required to confirm bar visibility and Ctrl-drain/regen.
