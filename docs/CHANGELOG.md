# Changelog

## 2026-05-12
- **0M-D5-02B-FIX4 (stamina regen tuning):** `PlayerStaminaController`: regen **~15s** for full 0→full at default max (`regen_rate_per_sec = max_stamina/15`); sprint drain **~2s** full bar while sprinting (`drain_rate_per_sec = max_stamina/2`, was 35/s). Earlier iterations 22→12.5→8 regen only. Reports: `docs/reports/taco_bell_d5_02b_fix4_stamina_regen_tuning/`; validator `src/tools/editor/taco_bell_d5_02b_fix4_stamina_regen_tuning/phase0md5_02b_fix4_static_validator.py`.
- **0M-D5-02B-FIX3 (stamina regen):** `PlayerStaminaController.process_frame` regen clamp **`mini` → `minf`** so float stamina accumulates each physics frame (integer `mini` was truncating sub-unit regen, so the bar often never refilled after Ctrl sprint). Reports under `docs/reports/taco_bell_d5_02b_fix3_stamina_regen/`; validator `src/tools/editor/taco_bell_d5_02b_fix3_stamina_regen/phase0md5_02b_fix3_static_validator.py`. No HUD / input / scene edits in this pass.

## 2026-05-11
- **0M-D5-02B-FIX2 (stamina bar visibility / data):** `MissionHudDataProvider` no longer treats `get_sprint_runtime_debug()["ok"]` as the sole gate — Player replaces the initial dict with the stamina-chain snapshot **without** re-adding `ok`, so `ok` was always false. Visibility now keys off **`current_stamina` + `max_stamina`** (or legacy `ok`), with **in-mission placeholder 100/100** when player/snapshot is missing. Payload adds **`stamina_fallback`**. `HUD.gd` sets a short **tooltip** when in fallback. `hud.tscn`: **StyleBoxFlat** fill/background + **min size 160×12** for contrast. Reports: `docs/reports/taco_bell_d5_02b_fix2_stamina_bar/`; validator `src/tools/editor/taco_bell_d5_02b_fix2_stamina_bar/phase0md5_02b_fix2_static_validator.py`.
- **0M-D5-02B-FIX1 (HUD visibility / runtime truth):** Corrected **CanvasLayer ordering** (higher layer draws on top; player HUD stays **50**, below Phase0J **90** / IsoMissionDebugPanel **100** when toggled). **`HUD._refresh_mission_compact_hud`**: compute payload and **objective ticker before** requiring `MissionHudStrip`; `_player_facing_objective_line()` adds a readable **Objective:** prefix. **`MissionHudDataProvider`**: reject `uid://`, `res://`, and `::`-call-shaped strings as player-facing objectives. **`hud.tscn`**: HUD layer **50**, explicit **`anchor_left = 0`** on objective band. **`PlayerSprintDebugOverlay`**: F11 layer **120 → 110** (debug tier below pause **120**). **`LevelBase`**: `_ensure_common_ui()` before `QuestManager.set_objective()`; **`HUD.gd`**: `mission_started` + deferred refresh + `PROCESS_MODE_ALWAYS` (from earlier D5-02B pass). Reports: `docs/reports/taco_bell_d5_02b_fix1_hud_visibility/`; validator `src/tools/editor/taco_bell_d5_02b_fix1_hud_visibility/phase0md5_02b_fix1_static_validator.py`. **GRB:** bridge not connected this session — runtime PASS not claimed for in-engine visibility.

## 2026-05-10
- **0M-D2A-FIX1 (sprint runtime):** `sprint` input is **Ctrl-only**; **Space stays on `dodge`** only. `PlayerStaminaController.is_sprint_requested()` adds **`Input.is_physical_key_pressed(KEY_CTRL)`** fallback (InputMap-only Ctrl often fails). `Player.gd` applies **`get_speed_multiplier()`** only when **`is_sprint_active()`** after `process_frame`; **`get_sprint_runtime_debug()`** for tooling. Reports `docs/reports/mission_foundation_d2a_fix1_sprint_runtime/`; validator `phase0md2a_fix1_sprint_runtime_validator.py`; harness `PlayerSprintRuntimeTest_0MD2A_FIX1.tscn`. D2A static validator updated for Ctrl-only sprint policy.
- **0M-D2A (sprint input canonicalization):** Fixed D2 `sprint` map: **4194324 was KEY_PAGEDOWN**, not Ctrl — `sprint` now uses **Space (32)** + **KEY_CTRL (4194326)**. `PlayerStaminaController.is_sprint_requested()`; `Player.gd` removes `(not combat_on)` stamina gate (stamina sprint was impossible with default combat) and **suppresses** stamina sprint during **combat dash** / **legacy dodge burst** so dash/dodge velocity is not stacked with sprint multiplier. Reports under `docs/reports/mission_foundation_d2a_sprint_fix/`; validator `phase0md2a_sprint_static_validator.py`; harness `PlayerSprintInputTest_0MD2A.tscn`.
- **0M-D2 (mission foundation):** Centralized **`MissionSceneResolver`** (`class_name`, non-autoload) for playable vs legacy vs classic Taco paths; **`HideoutMissionBoardController`**, **`HideoutStationCatalog`**, **`SceneManager.start_mission`**, **`IsoMissionDebugPanel` restart**, and **pending-mission scene match** now use the resolver (catalog `GameState.mission_catalog` still keeps classic `TacoBellMission.tscn` for non–hideboard flows). Added **`MissionPauseDataProvider`** + **`MissionClueBridge`** / **`MissionSchemeBridge`**; **`MissionObjectiveBridge.get_bridge_id`**. **`pause_menu.gd`** reads objectives/scheme/clues via provider + scheme bridge for unlocked ids. **`project.godot`**: added **`sprint`** (Ctrl, `physical_keycode` 4194324) only — backup under `docs/reports/mission_foundation_d2/backups/`. Static validator `src/tools/editor/mission_foundation_d2/phase0md2_static_validator.py`; test scenes `MissionSceneResolverTest_0MD2`, `MissionPauseDataProviderTest_0MD2`, `PlayerSprintInputTest_0MD2`. Reports: `docs/reports/mission_foundation_d2/phase0md2_*`. **GRB:** not connected in this environment (runtime report PARTIAL).
- **0M-D1C (emergency Taco playable routing):** Corrected **MissionBoard / hideout catalog** and **`SceneManager.start_mission` debug iso override** + **`IsoMissionDebugPanel` restart** so `taco_bell_drop` loads **`TacoBellIso_Editable_RedesignTest.tscn`** (expanded map evidence: larger scene, Phase0J/K markers). **`TacoBellIso_Editable.tscn`** kept as **legacy/bake-output** (unchanged on disk). Updated `DECISIONS.md`. Added `docs/reports/taco_canonical_scene_correction/*` + `phase0md1c_scene_compare.py` + `phase0md1c_static_validator.py`. RT static validator now also requires **RedesignTest** scene file to exist.
- **0M-D1B-RT2 (GRB recovery + in-engine smoke):** Recovered MCP **Godot Runtime Bridge** via `grb_reset` / `grb_launch` (tier-2 CONTROL) using Cursor companion Godot; verified **HideoutHub** load, **MissionBoard** panel (`open_station("mission_board")`), and **canonical** `TacoBellIso_Editable.tscn` via `SceneManager.change_scene` after `GameState.start_mission("taco_bell_drop")`. **Aligned hideout launch** with D1B canonical decision: `HideoutMissionBoardController` + `HideoutStationCatalog` `TACO_BELL_SCENE` → `TacoBellIso_Editable.tscn`. GRB synthetic input did not move the player token; calling `PauseMenu._pause_game()` stalled the bridge (tree paused). Reports: `docs/reports/pre_taco_module_hardening_runtime/phase0md1b_rt2_*`. Static: `phase0md1b_rt2_static_validator.py` + existing D1B/RT validators **PASS**. Verdict **PARTIAL** (see RT2 runtime report).
- **0M-D1B-RT (runtime smoke / regression gate):** Added RT static preflight `phase0md1b_rt_static_validator.py` and reports under `docs/reports/pre_taco_module_hardening_runtime/`. Re-ran D1B static validator (**PASS**). **MCP Godot runtime bridge** not connected (`grb_ping`); **no local Godot binary** found for headless run — in-engine playtest **PARTIAL** / deferred to local editor. **No D1B regression code fixes** required by static checks.
- **0M-D1B (pre–Taco Bell module hardening):** Removed `TacoBellDialogue` preload from `IsoMissionBase.gd`; added `MissionDialogueProvider` + `TacoBellDialogueProvider` (taco `mission_id` only). Added `MissionToolSurfaceHelper` and `IsoMissionBase.supports_tool` / `handle_tool_use`; `Player.gd` poop throw goes through helper (fixes double `poop_bags_used` increment vs `deploy_poop_bag_decoy_at`). Added `MissionObjectiveBridge` wrapping `QuestManager.set_objective` from Iso. Added `PlayerStaminaController` + minimal sprint multiplier wiring (optional `sprint` input — `project.godot` not modified). Added `reset_mission_runtime_for_new_attempt()` contract on Iso. Reports under `docs/reports/pre_taco_module_hardening/`; static validator `phase0md1b_static_validator.py`; test scene `PlayerStaminaControllerTest_0MD1B.tscn`. **Bake-output / pipeline Taco scene:** `TacoBellIso_Editable.tscn` (see **0M-D1C** for playable expanded-map routing). **Not modified:** Taco `.tscn`, `player.tscn`, `HideoutHub`, `project.godot`.

## 2026-05-09
- **0M-C2B-FIX1 (diagnostic only):** Added `character_animation_c2b_fix1_context_classifier_pipeline.py` — full **200×200** composite catalog (2500 frames), **21-frame** motion context features (numpy), paginated context window PNGs, manual seed regions, row browser, **segment** JSON/CSV + segment contact sheets/GIFs (GIF count capped with index note), recommended **clip** folders, diagnostic `parmida_context_classifier_spriteframes.tres` + atlas, reports under `docs/reports/character_animation_c2b_fix1/`, sandbox `CharacterAnimationContextClassifierSandbox_0MC2B_FIX1` (runtime SpriteFrames load). Validators: `character_animation_c2b_fix1_context_classifier_validator.py`, `CharacterAnimationC2BFix1ContextClassifierValidator.gd`. **Requires `pip install numpy` for the Python pipeline.** Production player / Taco Bell / raw kit unchanged.
- **0M-C2B (sandbox-only discovery):** Added `character_animation_c2b_discovery_pipeline.py` for **200×200** row forensics (per-row + master contact sheets), row classification, conservative idle/walk/run/attack selection (`RUN_NOT_FOUND_SAFE` / `ATTACK_NOT_FOUND_SAFE` when no safe row), generated `c2b_full_animation/` (`parmida_player_spriteframes_0mc2b.tres`, composite atlas, frames, metadata). Reports under `docs/reports/character_animation_c2b/`. New sandbox `CharacterAnimationSandbox_0MC2B` loads C2B SpriteFrames **at runtime** (no `.tres` ExtResource on scene), compares static Parmida + FIX3 idle + C2B, with frame stepping and explicit missing-animation labels. Added `character_animation_c2b_static_validator.py`, `CharacterAnimationC2BValidator.gd`, and future production controller design doc. **Production `player.tscn` / `Player.gd` / Taco Bell unchanged.**
- **0M-C2A-FIX3 (sandbox-only):** Added `character_animation_c2a_fix3_pipeline.py` for **100×200 vs 200×200** frame-size forensics (contact sheets + decision), rebuilt Parmida animation under `c2a_animation/fix3_stable/` using **200×200** cells aligned with C2 `source_frame_rect`, **idle-first** pruned frames, optional walk rejected when unstable; new `parmida_player_spriteframes_0mc2a_fix3.tres`. Updated `CharacterAnimationSandbox_0MC2A` controller for **FIX3 primary**, FIX2/FIX1 as fallbacks only, and a **frame-by-frame viewer**. Added `character_animation_c2a_fix3_static_validator.py`, `CharacterAnimationC2AFix3Validator.gd`, and `docs/reports/character_animation_c2a/phase0mc2a_fix3_*` reports. **Production `player.tscn` / `Player.gd` unchanged.**

## 2026-05-03
- **Taco Bell player-facing gameplay cleanup (Phase 2A.5):** Kept editable hybrid authoring contract intact while improving in-scene testability in `TacoBellIso_Editable.tscn`.
- **Interaction priority hardening:** Player interaction scan now filters unavailable prompts, strongly deprioritizes completed nodes, and prefers higher-priority incomplete interactables to reduce nearby used-object steals.
- **Dev launch + harness:** Debug mission launch now routes `taco_bell_drop` to `TacoBellIso_Editable.tscn`; `IsoMissionDebugPanel` now reports real scent route, active garage code, extra guard/camera booleans, and typed collectible counts.
- **Gameplay feedback polish:** Added explicit clue pickup feedback line; code-gate wrong-attempt feedback now includes attempt count; route access messaging now has clear Louis locked/unlocked and vent test-route prompts.
- **Stealth/combat visibility:** `MissionAlertController` now folds cover tiles into detection modifier, and guards render a debug cone in debug builds; validator now checks guard combat compatibility against player attack pipeline.
- **Validator expansion for player-facing QA:** Added `clustered_interactables`, `overlapping_interaction_radii`, `priority_conflicts`, `guard_combat_compatibility`, and `heat_test_harness_available` fields/checks with required-system failures as errors.
- **Taco Bell editable scene blocker closure (Phase 2A.4B):** Fixed duplicate marker IDs in baked `MarkerRoot` with canonical marker id mapping and deterministic `_NN` suffixing; added bake-time marker dedupe + marker position normalization.
- **Hybrid source-of-truth enforcement:** `IsoMissionBase` now builds a marker index at runtime and resolves positions marker-id-first/link-first (`linked_clue_id`, `linked_collectible_id`, `linked_objective_id`, `linked_route_id`, `linked_guard_id`) before definition fallback, and emits per-object resolution telemetry (`object_id`, `object_type`, `resolved_from`, marker id, marker position, spawn position, delta).
- **Hand-edit proof fixes:** Moved clue and moved poop bag in `TacoBellIso_Editable_Test.tscn` now spawn from moved marker positions with zero runtime mismatch; moved guard/patrol/exit marker proofs now resolve from scene markers as expected.
- **Validator trustworthiness pass:** duplicate id check now reports unique duplicates; hybrid marker ignores and runtime position mismatches are hard errors; required interactable checks now locate runtime objects by ids/properties; exit expected-cell check reads scene exit marker id; enemy iso profile check validates actual runtime guards and reports expected/actual scale + collision.
- **Noise scoping for editable scenes:** boundary surround and transition blocker checks remain visible as warnings in editable scenes instead of hard fail, while core authoring safety checks stay strict.
- **Taco Bell editor bake workflow (Phase 2A.4A):** Added `TacoBellIso_Editable.tscn` and `TacoBellIso_Editable_Test.tscn` generation pipeline from `TacoBellIsoBlockout`; added explicit editable `LayoutRoot` tile layers (`FloorLayer`, `WallLayer`, `CoverLayer`, `CollisionBarrierLayer`, `MarkerTileLayer`, `DebugLabelLayer`) and `MarkerRoot` category buckets.
- **Authoring marker upgrade:** Added `src/missions/iso/authoring/IsoMissionMarker.gd` (`@tool`) with inspector-editable marker metadata, radius/zone sizing, and editor labels; `IsoAuthoringMarker` now inherits this script for compatibility.
- **Bake tooling:** Added `src/tools/BakeIsoMissionToEditableScene.gd` and new `IsoMissionBase` bake APIs (`bake_to_editable_scene`, `bake_hand_edit_test_scene`) to create/update editable scenes safely.
- **Runtime + validator hybrid support:** `IsoMissionBase` now syncs scene-authored `LayoutRoot` layers to gameplay layers in hybrid/scene-authored mode, reads markers recursively from `MarkerRoot`, and emits richer source-of-truth debug metadata. `MissionBlockoutValidator` now reports editable-scene fields (scene path, marker/layer counts, duplicate marker IDs, marker placement issues, runtime position mismatches).
- **Taco Bell editor-authorable + player-facing testability pass (Phase 2A.4):** Added explicit mission `authoring_mode` (`generated`/`scene_authored`/`hybrid`) and set Taco Bell to `hybrid`; introduced `IsoAuthoringMarker` node contract for scene marker placement; preserved scene-authored gameplay layout in hybrid/scene-authored modes; added route test subareas/spawn markers and transition hooks.
- **Iso system testing harness:** Added debug-only `IsoMissionDebugPanel` with heat controls, mutation reroll, alarm/extra-guard triggers, scheme-card route toggles, poop-bag controls, mission performance reset, and teleport shortcuts to key Taco Bell test zones.
- **Interaction/combat/feedback usability:** Updated player interact scan to use per-object availability + priority; added priority hooks to placeholders/runtime interactables; added wrong/correct keypad input window for code-gate testing; added collectible pickup feedback for Glow Guy/Tiny Icon/Poop Bags; added combat debug lines (`Player attacked`, `Guard hit`, `Guard defeated`).
- **Runtime visibility + validator expansion:** Added detection state signal for HUD debug meter, camera cone debug visualization, camera-shake debug logging, guard iso profile validation, authoring marker validation, overlap/priority conflict checks, and richer runtime debug report fields (`authoring_mode`, marker sources/counts, enemy iso scale, overlapping interactables).
- **Dev launch override:** In debug builds, mission-board launches of `taco_bell_drop` can route to `TacoBellIsoBlockout.tscn` via `GameState.dialogue_flags["dev_force_iso_taco_bell"]` without permanently changing release story mission wiring.
- **Taco Bell reusable gameplay systems (Phase 2A.3):** Added additive runtime mission systems for iso blockouts: `RuntimeSystems` hierarchy in `IsoMissionBase`, marker-driven guard spawn + patrol path, security camera with detection proxy, alert/alarm controller, shadow/bright/flicker light zones, ambush/encounter trigger, route-access placeholders, transition placeholder trigger, poop-bag decoy point, camera terminal disable hook, and camera shake utility. Taco Bell now validates with runtime system counts and retains all Phase 2A.2 layout/clearance fixes.
- **GameState additive persistence layer:** Added backward-compatible structured maps for scheme cards, evidence clues, typed collectibles, crew assists, mission heat snapshots, mission performance, mission alert states, and poop bag inventory while preserving legacy mission flow and save compatibility.
- **Phase 2A.3 typed metadata wiring:** Expanded mission definition/resource fields for richer clue/collectible/reward/route metadata and updated Taco Bell mission definition with clue-board clusters, reward effect data, and collectible grouping flags.
- **Taco Bell return-route clearance fix:** Widened the bag-room drop, south return corridor, and west exit approach after manual testing found grid-reachable bottom passages that were too tight for the player body. Added required-passage and exit-approach width validation, exposed route-width debug fields, and applied an iso-only player token profile (`0.86` visual scale, `24x24` collision) without touching the base player scene.
- **Taco Bell start movement fix:** Widened the Louis start plaza, moved the player spawn away from the Louis objective marker, and shifted/shrank generated wall-following boundary colliders outward so they no longer intrude into the spawn pocket. Added validator checks for 3x3 spawn floor clearance, start-zone minimum size, Start-to-Market entrance width, and blocking bodies near spawn; smoke testing now starts with real movement input from the actual spawn.
- **Taco Bell iso map reshape Phase 2A.2:** Rebuilt the blockout topology into a 15-zone west-start/east-garage/south-return loop with explicit security booth, return drop, long south corridor, and west-side return-to-Louis exit. Boundary colliders now generate from exterior wall cells, cover cells are validated as colliding blockers, cutscene triggers no longer steal E interaction, and validator coverage now checks marker floor placement, required interactable setup, interactable overlap risk, Taco Bell branch topology, return loop placement, and cover collision.
- **Taco Bell iso layout Phase 2A.1:** Reworked the gold-standard blockout from a straight zone strip into a branched graybox layout: open Louis start, wider market street, optional dog-station branch, delivery-alley scent fork, fake scent side branches, real garage entrance, larger garage floor, distinct side office/keypad room, ambush room, bag room, and lower return path. Added a Taco Bell layout profile for interior cover/choke collision while keeping ArtRoot non-gameplay.
- **Taco Bell iso blockout Phase 2A:** Expanded `taco_bell_iso_blockout_definition.tres` into a 12-zone gold-standard candidate with 8 primary objectives, optional Mission Bible beats, four required Sterling clues, expanded collectibles, reward metadata, mutation pools, stealth/combat/heat placeholders, and final tower logistics connections. `TacoBellIsoBlockout.tscn` remains additive and is still not wired into `GameState.mission_catalog`.
- **Iso framework gameplay hooks:** Added reusable scent-trail and code-gate placeholders; generated labels for zones/markers; made once-only placeholders leave the player interact scan after completion; kept exit walk-in/E completion debounced and readable.
- **Validation:** `MissionBlockoutValidator` now requires a final tower delivery/logistics dependency and the expanded Taco Bell scene passes reachability, boundary, exit, collision-layer, and completion-path checks.

## 2026-05-02
- **Repo hygiene:** Ignore `debug/` (GRB launch logs, screenshots); removed accidentally committed artifacts from version control. Root-level `debug-*.log` ignore unchanged.

- **Iso mission blockout framework (Phase 1):** Added typed `MissionDefinition` resources, `IsoMissionBase.gd`, semantic iso blockout TileSet/atlas, canonical template scene, generic placeholder interactables/components, `MissionBlockoutValidator.gd`, docs, and an isolated Taco Bell iso blockout prototype under `scenes/missions_iso/`. Existing story missions and `GameState.mission_catalog` flow remain unchanged.

- **Iso cyberpunk vertical slice:** Monogon subset → `assets/tilesets/iso_vertical_slice/processed/cyberpunk_iso_atlas.png` + `IsoCyberpunkVerticalSlice.tres`; `IsoVerticalSlice.gd` paints floors/walls/detail door/props; `source/monogon/.gdignore` skips bulk Godot imports; **`ASSET_MANIFEST.md`** added with license TODO. Prototype `prototype_iso_atlas.png` / `IsoVerticalSlice.tres` retained.
- **IsoVerticalSlice reliability:** West spawn vs east exit, delayed **`ExitZone.monitoring`**, frame-separated **`PropLayer`** paints, runtime **`TileSet`** fallback when atlas import/`.ctex` is missing; `TileSet.TILE_LAYOUT_STACKED` / `TILE_OFFSET_AXIS_HORIZONTAL` in runtime builder (matches `.tres`).

- **Isometric dev slice (isolated):** `scenes/dev/IsoVerticalSlice.tscn` + `IsoVerticalSlice.gd` — standalone `TileMapLayer` stack under `WorldRoot` (`GroundLayer`, `DetailLayer`, `WallLayer`, `PropLayer`, `EntityRoot`) with `y_sort_enabled`; runtime tile paint from temporary `assets/tilesets/iso_vertical_slice/` placeholder atlas + `IsoVerticalSlice.tres`; `GameState.mission_catalog` entry `iso_vertical_slice` (**not** in default `available_missions`). `player.tscn` **collision_mask** `3` → `7` so **layer 3 (Walls)** collides with `CharacterBody2D` (required for wall tiles without `Player.gd` changes). See `docs/ISOMETRIC_LEVEL_SPEC.md`.

## 2026-05-01
- **Velvet Paw Floor 2 (Phase 3):** `JazzClubMission.tscn` — `Floor2Visuals` backstage/VIP/sound-booth layout at `y=1664`; repositioned `BackstageArea`, rig stairs (`OwnerSuiteStairs`, `StairsWayfinding`, `UpstairsFloor`, `StairsBlocker`), balcony briefcase markers, mixer/VIP phone/goblin polaroid; `SoundBoothShipmentNote` readable manifest; `Floor2PlayerSpawn` marker for Phase 4; `StaffBadgePickup` + `StaffRouteZone` remain Floor 1 for pre-puzzle social stealth.
- **Mission Bible implementation pass:** Save version `0.4.0-bible` — `mission_mutation_state`, `sterling_clues`, `poop_bag_count`, serialized `velvet_paw_resume_data`; heat API `get_mission_heat`, mutation rolls, Responsible Crime Lord intel bonus (3 poop bags/run); `CollectibleManager.has_polaroid`; legacy polaroid ID migrations (`rewrite_room_proof`, `conservatory_serenity`, `ellie_rescue`, `shadow_solo_pose`).
- **Shared pickups:** `PoopBagPickup` + `scenes/collectibles/PoopBagPickup.tscn`, `spawn_poop_bags_at_global_positions` on `LevelBase`; `MissionMutationHelper`; contaminated manifest interact (`contaminated_evidence_pickup.gd`).
- **Evidence board:** Sterling clue cards with `sterling_clue_id` persistence, auto-import of discovered clues, link line drawing.
- **Taco Bell:** Mutation keypad codes + Glow Guy slots, heat extra patrol, Louis heat hint, Tiny Icon + 3 poop bags + optional contaminated pickup, perfect polaroid, structured Sterling clue.
- **Jazz Club:** Extra pickups (hidden stage polaroid, bathroom Glow Guy, Yordano Tiny Icon, 3 poop bags), ledger clue registration, perfect polaroid when no setlist alarm; upstairs boss flow unchanged.
- **All missions:** Poop bag spawns and/or mutation rolls where applicable; Persian Tea community clue; polaroid ID alignment for Rewrite / Elephant / Shadow hidden pickups.

## 2026-04-30
- **Legal-safe collectibles:** Renamed optional polaroid IDs `taco_bell_smiskis` → `taco_bell_glow_guys`, `velvet_smiskis` → `velvet_shelf_goblins`; `GameState.normalize_polaroid_id` + save migration; hideout shelf uses `GlowCollectibleShelf` / `SceneManager.open_glow_collectible_shelf`; `debug_unlock_all_missions` defaults **false** for story order.

## 2026-04-29
- **Combat / HUD:** Light attack bound to **J** (plus existing mouse/gamepad); Bentley dental save adds 500ms damage grace so multi-hits do not apply HP after the proc; enemies get `hit_recovery_delay` after being hit; boss HP bar offset raised; HUD **STYLE** bar + finisher hint; `EventBus.combat_style_changed`.
- **Debug / UX:** Club owner boss shows a floating HP bar (`BossFloatingHealthBar`), chases slower (`chase_speed` 60) for troubleshooting; `GameState.debug_unlock_all_missions` (default false since 2026-04-30) appends all `mission_catalog` IDs after `reset_for_new_game` / new game when enabled — saves are unchanged; automated unlock tests force the flag off during runs. Pause combat text notes dash needs a move direction.
- **Player combat integration:** `PlayerCombatController` + `DashAbility` + `MeleeHitbox` under `player.tscn`; hitbox-based light combo / heavy (Q or RMB) / style finisher (R) / dash i-frames (Space); stealth overlap + unaware enemies supports `instant_kill()`; `EnemyBase` exposes `is_aware()` / `instant_kill()`; `CardEffects` adds combo window, style decay reduction, finisher damage hooks; `CardManager.is_card_active()` aliases selection; `EventBus.screen_shake` for combat juice (listeners optional). Legacy range `_attack()` + dodge timers remain only if `PlayerCombatController.enabled` is false.
- **Jazz owner-suite boss arena:** New scene `JazzClubOwnerArena.tscn`—press **E** on `OwnerSuiteStairs` (after setlist + both clues) to load the fight; bruiser boss with intro/outro dialogue; mission state saved in `GameState.velvet_paw_resume_data`; victory returns to the club at `SpawnPoints/ReturnFromOwnerSuite` and unlocks the balcony briefcase pickup.
- **Jazz Club overhaul:** 5-slot setlist with decoys + album-order hint; readable notes render on a high `CanvasLayer` (no top-left clipping); wrong-setlist triggers bass alarm (red overlay, reinforced `guard.tscn` hunters, safe-spot objective copy, 8s resolve); clues repositioned on bar vs dance floor and green sniff-trail shortcut removed; solving setlist opens stairs/backstage blockers.
- **Input / interact:** `Player` advances dialogue with E from `_physics_process` (fullscreen UI was eating `_unhandled_input` before `DialogueBox`, soft-locking the world); modal note + music puzzle use `blocking_ui` on their `CanvasLayer` with E/Esc to close; `DialogueManager.next_line` debounced to avoid double-advances.
- **Jazz Club fix:** Disabled invisible `LedgerDecoy` interactable/collision (it was stealing [E] backstage and showing a ledger the real upstairs flow could not complete); added `StairsWayfinding` (rig stairs treads + hint) revealed when the setlist is solved so the 2F route is visible.
- Velvet Paw (Jazz Club): perimeter + shortcut seal + tall backstage seal (puzzle removes seal); staff south gate optional path after badge pickup; ledger decoy twist with real book under amp; staff badge + sound check micro-objectives; interactables (mixer, VIP phone, Sterling poster); optional `velvet_shelf_goblins` and bar polaroid pickups; clue notes advance clue count via `note_read`.
- Scheme Cards: HUD shows per-card title, description, and READY/ACTIVE/USED state with short toasts when effects fire (`EventBus.card_triggered`).
- Taco Bell mission: wired passive card outcomes — Louis delivery route (service door without keycard), Mere legal eyes (one-shot alarm bypass), Polaroid Proof (start with receipt code), Clorox Protocol (skip decoy twist), Diamond A Year (highlight loot + marker). Passive combat/movement cards announce ACTIVE on deploy.

## 2026-04-28
- Added creative direction, architecture, task list, and decision docs.
- Confirmed MVP/stretches are separated around the heist spine.
- Continued implementation toward Nocturne City hub and final payoff.
- Repaired and polished City Hub as a contact/mission bridge from the hideout.
- Added final ending payoff scene after Sterling Tower.
- Improved failure progression with rotating titles, intel, card hints, and Jake upgrade support.
- Added six stretch mission maps using a shared interactive mission framework.
- Added dedicated Bentley ability input, mission ranks, camera follow for larger maps, and safer Scheme Card validation.
- Started neon-noir theme polish and distinct placeholder tinting for Parmida and Bentley.
- Added modules for branching dialogue, readable notes, evidence board, stealth vision, patrol routes, cipher puzzles, objective chains, room transitions, loadouts, and polaroid pickups.
- Fixed lingering parser issues in Rewrite Room and guard visuals by replacing fragile polygon scene data with simple ColorRects.
