# Beam fix

**Deferred:** Full visible laser art pass (non-goal).

**Done:** Runtime truth and **F10 hint** (`garage_beam_f10_hint`, armed/triggered flags) so testers can find the **garage entry beam** zone path. Event path already maps to **`beam_trip`** once per attempt via runtime flags + adapter dedupe.
