# Beam / F10 audit

Runtime alarm zone is created under `GameplayRoot/RuntimeSystems/AlarmZones` with name pattern `AlarmZone_garage_entry_beam`, id `garage_entry_beam`. No dedicated laser mesh; zone-based trigger.
