# FIX3 — Animation frame selection

- **Mode:** **Idle-first**; **walk rejected** (no stable walk strip under FIX3 gates).
- **Cell size:** **200×200** (post-forensic).
- **Idle strip:** horizontal **row 31**, raw columns 0–9 scanned; **accepted** composited frames at source indices **4, 5, 6** (3 frames) for a short, stable loop.

JSON: `phase0mc2a_fix3_animation_frame_selection.json` (sync with `parmida_player_animation_metadata_0mc2a_fix3.json` after each pipeline run).
