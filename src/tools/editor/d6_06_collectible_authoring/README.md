# D6-06 Collectible Authoring Validator

This folder contains the original D6-06 collectible authoring static validator.

Status: superseded for new cleanup/feature decisions.

Reason:

- This validator checks the older `AuthoredCollectiblePickup.gd` path.
- Current runtime uses the Phase0J interactable path through `AuthoredPhase0JInteractablePickup.gd`.
- New work should use the later D6-06B interactable/hideout sync validator plus D6-07/D6-07B/D6-08A validators.

Keep this folder for historical phase evidence unless a later cleanup pass archives superseded validators.
