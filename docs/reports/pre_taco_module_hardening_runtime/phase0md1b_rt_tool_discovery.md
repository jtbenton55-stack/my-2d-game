# Runtime tool discovery (0M-D1B-RT)

| Step | Outcome |
|------|---------|
| `grb_ping` (Godot runtime bridge MCP) | **Not connected** — requires an already-running game with GRB |
| Local `Godot*.exe` search | **No binary found** in common Windows locations |
| PATH | **`godot` not available** in automation shell |

**Next step for full PASS:** launch the project locally (editor or `Godot*.exe --path <repo>`), then re-run manual checklist or connect MCP GRB.
