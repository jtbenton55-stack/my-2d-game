# Testing Scenes

This folder is for small runtime/editor harness scenes, not player-facing content.

## MCP Runtime Harness

- `McpRuntimeBlankTest.tscn`

This scene is intentionally kept as a minimal Godot MCP / playmode recovery harness. It has been used to diagnose runtime bridge and playmode issues without involving gameplay scene logic.

Do not delete it unless the MCP/GRB/DAP workflow no longer needs a minimal scene harness.
