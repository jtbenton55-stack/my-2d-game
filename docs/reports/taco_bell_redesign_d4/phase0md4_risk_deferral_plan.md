# 0M-D4 — Risk, deferral, do-not-overbuild plan

## What must ship before “redesigned Taco” is honest

**P0:** Attempt reset + objective/pause truth + Louis bypass **gameplay** effect + beam one-shot verification + validators (see `phase0md4_risk_deferral_plan.json`).

## What should ship in D5 if safe (P1)

Code gate regression harness, beam wiring audit, poop tuning via mission surface.

## What waits until after first playable slice (P2)

Clue/scheme flavor, extra dialogue, non-blocking scene marker cleanup.

## Future polish (P3)

Art pass, reward heat, advanced AI.

## Animation / combat / stealth

**Deferred:** Full combat, full stealth AI, animation-dependent gating — **no D5 work** unless audit reopens with new evidence.

## IsoMissionBase / save / scene risk

See JSON `risks` + `testing_to_prevent_regression`.

Assertions: plan created; P0–P3 done; animation deferred; combat/stealth deferred.
