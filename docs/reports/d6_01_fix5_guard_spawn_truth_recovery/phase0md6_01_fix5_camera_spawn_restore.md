# Camera visible spawn restore

- Camera source ids (garage_fallback_camera_%d) preserved and used for preferred spawn positioning.
- Spawn requests no longer consume cap when spawn is invalid/off-map.
- Runtime validation still required in editor for full PASS.
